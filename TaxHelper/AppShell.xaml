<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:TaxHelper"
       x:Class="TaxHelper.AppShell"
       FlyoutWidth="200"
       FlyoutBackgroundColor="#2196F3">
    <FlyoutItem Title="Lookup">
        <ShellContent Route="Lookup"
                      ContentTemplate="{DataTemplate local:TaxRateLookup}" />
    </FlyoutItem>
    <FlyoutItem Title="Calculator">
        <ShellContent Route="Calculator"
                      ContentTemplate="{DataTemplate local:TaxCalculator}" />
    </FlyoutItem>
    <FlyoutItem Title="Settings">
        <ShellContent Route="Settings"
                      ContentTemplate="{DataTemplate local:Settings}" />
    </FlyoutItem>
</Shell>
